<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
  <div class="col-xl-4 col-md-8 col-12">
    <div class="card shadow-lg">
      <div class="card-header text-center bg-primary text-white">
        <h2>Crear Manager</h2>
      </div>
      <div class="card-body">
        <%= form_with(model: @manager, url: managers_path, local: true) do |f| %>
          <% if @manager.errors.any? %>
            <div class="alert alert-danger">
              <h4>Se encontraron los siguientes errores:</h4>
              <ul>
                <% @manager.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="mb-3">
            <%= f.label :name, 'Nombre del Manager', class: 'form-label' %>
            <%= f.text_field :name, class: 'form-control' %>
          </div>

          <div class="mb-3">
            <%= f.label :user_id, 'User', class: 'form-label' %>
            <%= f.collection_select :user_id, User.all, :id, :username, prompt: "Selecciona un user", class: 'form-select' %>
          </div>

          <div class="mb-3">
            <%= f.label :predictor_id, 'Predictor', class: 'form-label' %>
            <%= f.collection_select :predictor_id, Predictor.all, :id, :id, prompt: "Selecciona un predictor", class: 'form-select' %>
          </div>

          <div class="d-grid gap-2">
            <%= f.submit 'Crear Manager', class: 'btn btn-primary btn-lg' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
