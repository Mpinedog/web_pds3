<h1 class="mb-4 my-title">Edición de controlador</h1>

<%= form_with(model: @manager, url: manager_path(@manager), method: :patch, local: true, html: { class: 'custom-form' }) do |f| %>
  <% if @manager.errors.any? %>
    <div class="alert alert-danger">
      <h4>Se encontraron los siguientes errores:</h4>
      <ul>
        <% @manager.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :name, 'Nombre del Controlador', class: 'form-label' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="d-grid gap-2">
    <%= f.submit 'Actualizar Controlador', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>