<!-- app/views/modelos/edit.html.erb -->
<div class="container mt-5">
  <h2>Editar Modelo</h2>
  <%= form_with(model: @modelo, local: true) do |f| %>
    <% (1..6).each do |i| %>
      <div class="mb-3">
        <%= f.label "sign#{i}", "Símbolo #{i}", class: 'form-label' %>
        <%= f.text_field "sign#{i}", class: 'form-control', value: @modelo.send("sign#{i}") %>
      </div>
    <% end %>

    <div class="mb-3">
      <%= f.label :archivos, 'Actualizar Archivos', class: 'form-label' %>
      <%= f.file_field :archivos, multiple: true, class: 'form-control' %>
    </div>

    <div class="mb-3">
      <%= f.label :figuras, 'Actualizar Figuras (símbolos)', class: 'form-label' %>
      <%= f.file_field :figuras, multiple: true, class: 'form-control' %>
    </div>

    <%= f.submit 'Actualizar Modelo', class: 'btn btn-primary' %>
  <% end %>
  <%= link_to 'Volver a la Lista de Modelos', modelos_path, class: 'btn btn-secondary mt-3' %>
</div>
